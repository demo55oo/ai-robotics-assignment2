# Remaining manual steps (team)

The code, bridge config, launch files, and GitHub repo scaffolding are ready. Finish these before the form:

1. **Obtain the instructor Gazebo car world** and set `WORLD_PATH`.
2. Practice until one clean in-lane lap (`v_cruise` start at 1.5–2.0).
3. **Record video** (Win+G): show bridge + node launches, then one full lap.
4. **Record bag** on that same run: `bash scripts/term4_bag.sh` → zip `cmd_vel_bag`.
5. Upload video + bag zip; sharing = anyone with the link.
6. Edit root `README.md`: team names/IDs, lap time, video link, bag link.
7. `git add README.md && git commit && git push`
8. Submit the GitHub URL: https://forms.gle/4SSA9fNCGhV3ynNx8
